<center>
 <div class="login-box">
    <p>
      <strong>Name:</strong>
      <%= @product&.name %>
    </p>

    <p>
      <strong>Price:</strong>
      <%= @product&.price %>$
    </p>

    <p>
    <% if @product&.product_image&.attached? %>
    <%= image_tag @product.product_image, height:150, width:150 %>
    <% end %>
    </p>

    <%= link_to 'Edit', edit_product_path(@product), class: 'btn btn-dark' %> |
    <%= link_to "Destroy", products_path(@product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  }, class: 'btn btn-dark' %></li>
    <br>
    <br>
    <%= link_to "Back", products_path, class: 'btn btn-dark' %>
  </div>
</center>
